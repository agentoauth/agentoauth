{
  "name": "agentoauth",
  "version": "0.7.0",
  "private": true,
  "description": "A neutral protocol for AI agent authorization",
  "scripts": {
    "setup": "pnpm install && pnpm run build",
    "build": "pnpm --filter @agentoauth/sdk build && pnpm --filter @agentoauth/verifier-api build",
    "build:all": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "demo": "pnpm install && pnpm run build && cd packages/demo-agent-to-merchant && bash demo.sh",
    "demo:merchant": "cd packages/demo-agent-to-merchant && node merchant.js",
    "demo:agent": "cd packages/demo-agent-to-merchant && node agent.js",
    "test:conformance": "cd packages/conformance && pnpm build && pnpm test:local",
    "test:conformance:remote": "cd packages/conformance && pnpm build && pnpm test:remote",
    "quickstart:node": "cd quickstarts/node-express && npm install && npm run demo",
    "quickstart:cf": "cd quickstarts/cloudflare-workers && npm install && npm run dev",
    "hosted:generate-keys": "cd packages/hosted-verifier && npm run gen-keys",
    "hosted:deploy": "cd packages/hosted-verifier && wrangler deploy --env production"
  },
  "keywords": ["oauth", "agent", "authorization", "jwt", "jws"],
  "author": "",
  "license": "MIT AND Apache-2.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
